name: hello world
on: workflow_dispatch
jobs:
 build:
   runs-on: ubuntu-latest
   steps:
      - name: step1
        run: echo" hii hruthik"
test:
    runs-on: ubuntu-latest
    steps: 
      - name: step2
        run: eho "nin nkn"
      
    
