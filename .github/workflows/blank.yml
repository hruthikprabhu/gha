name: hello world
on : workflow_dispatch
jobs:
  build:
     runs-on: ubuntu-latest
     steps:
         - name: hello step
           run: echo "welcome to gha"
  test:
     runs-on: ubuntu-latest
     steps:
        - name: test step
          run: echo "test is done "
  deploy:
      runs-on: ubuntu-latest
      steps:
         - name: deploy step
           run: echo "deploy is docne"
